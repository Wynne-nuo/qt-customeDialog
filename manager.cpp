#include "manager.h"
#include "qwdialogsize.h"
#include <QDebug>

manager::manager()
{
//    connect(m_size, SIGNAL(signal_sendRowCol(int,int)), this, SLOT(slot_getRowCol(int,int)));
}

//void manager::slot_getRowCol(int row, int col)
//{
//    qDebug()<<"manager接收到的行列值为"<< row <<col;
//}
